<UserControl x:Class="WPFPhysicsControlsLib.Controls.DebugTouchPointsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:WPFPhysicsControlsLib.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModel:SimulationVm}">
    <Canvas Background="Transparent" IsHitTestVisible="False">
        <ItemsControl ItemsSource="{Binding CurrentlyDetectedTouchPoints}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Ellipse Width="20" Height="20" Stroke="Plum" StrokeThickness="3">
                        <Ellipse.RenderTransform>
                            <TransformGroup>
                                <TranslateTransform X="{Binding X}" Y="{Binding Y}"/>
                                <!--<ScaleTransform ScaleX="{calcBinding:Binding 0.5f * Position.X}"></ScaleTransform>-->
                            </TransformGroup>
                        </Ellipse.RenderTransform>
                    </Ellipse>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Canvas>
</UserControl>
